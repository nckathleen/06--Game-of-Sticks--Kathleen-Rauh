import unittest
from sticks import *

class TestSticks(unittest.TestCase):


def sticks_taken(self):
    pile = 20
    self.assertEqual(player_turns(sticks_removed,, [4]), "_ _ _ _ _ _ _ _ _ _ _")
    self.assertEqual(player_turns(sticks_removed, ["z"]), 
    self.assertEqual(player_turns(sticks_removed, ["33"]),
    self.assertEqual(player_turns(sticks_removed,
    self.assertEqual(player_turns(sticks_removed,
    self.assertEqual(player_turns(sticks_removed,


def sticks_on_table():


def
